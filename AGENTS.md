# Suzannah Pugh Music Studio

- `src/data/studio.ts` is the editable source of studio facts. Preserve `[CONFIRM: ...]` and `[ADD: ...]` markers until verified.
- Do not invent credentials, policies, availability, pricing, contact details, or student-facing claims.
- Do not edit `dist/`; update source files instead.
- Verified command: `npm run validate`.
